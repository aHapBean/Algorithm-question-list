//#pragama optimize(2)
#define fi freopen("in","r",stdin);
#define debug(x) cout << "here" << x << "\n"
#include <bits/stdc++.h>
using ll = long long;
using namespace std;
const int maxn = 2e5 + 5;
struct record{
	int ind,x,y;
}rec[maxn];
int n,cnt,x,y,t;// -> a . b < 0 or > 0

vector<int> cir[maxn];
int tag[maxn];
int main(){
	cin >> n;
	for(int i = 1; i <= n; ++i){
		scanf("%d%d%d",&t,&x,&y);
		if(t == 1){

		}
	}
	return 0;
}
